我已找到问题所在。在之前的修改中，`MindMap` 组件的接口定义增加了 `ratio` 和 `res` 两个必填参数，但在 `App.tsx` 中调用该组件时未传递这两个参数，导致 TypeScript 类型检查失败或运行时错误，进而使页面渲染空白。

### 修复计划
1.  **修改 `fronted/App.tsx`**：
    *   在 `<MindMap />` 组件的调用处，补充传递 `ratio={ratio}` 和 `res={res}` 属性。

### 验证计划
*   修改完成后，应用应能自动恢复正常显示。
*   点击左侧面板的 "Open MindMap" 按钮，确认思维导图能正常弹出且不再导致白屏。